[plugin]
name = "cors"
version = "0.1.0"
type = "middleware"
description = "CORS middleware for cross-origin resource sharing"
wasm = "cors.wasm"

[capabilities]
host_functions = ["log", "context_get", "context_set"]
