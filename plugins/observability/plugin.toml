[plugin]
name = "observability"
version = "0.1.0"
type = "middleware"
description = "Per-operation observability middleware for SLO monitoring, detailed logging, and custom metrics"
wasm = "observability.wasm"

[capabilities]
host_functions = ["log", "context_get", "context_set", "telemetry", "clock_now"]
