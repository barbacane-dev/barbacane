[plugin]
name = "apikey-auth"
version = "0.1.0"
type = "middleware"
category = "authentication"
description = "API key authentication middleware for validating API keys from headers or query parameters"
wasm = "apikey-auth.wasm"

[capabilities]
host_functions = ["context_set"]
