[plugin]
name = "response-transformer"
version = "0.1.0"
type = "middleware"
description = "Transform HTTP responses: status code, headers, and body"
wasm = "response-transformer.wasm"

[capabilities]
log = true
