[plugin]
name = "oauth2-auth"
version = "0.1.0"
type = "middleware"
description = "OAuth2 token introspection middleware for validating Bearer tokens via RFC 7662"
wasm = "oauth2-auth.wasm"

[capabilities]
http = true
