[plugin]
name = "jwt-auth"
version = "0.1.0"
type = "middleware"
category = "authentication"
description = "JWT authentication middleware for validating Bearer tokens"
wasm = "jwt-auth.wasm"

[capabilities]
host_functions = ["verify_signature", "clock_now", "log", "context_set"]
