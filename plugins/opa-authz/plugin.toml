[plugin]
name = "opa-authz"
version = "0.1.0"
type = "middleware"
description = "OPA authorization middleware — policy-based access control via Open Policy Agent"
wasm = "opa-authz.wasm"

[capabilities]
host_functions = ["http_call"]
