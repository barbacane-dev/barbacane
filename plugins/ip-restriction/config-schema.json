{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:barbacane:plugin:ip-restriction:config",
  "title": "IP Restriction Middleware Configuration",
  "type": "object",
  "properties": {
    "allow": {
      "type": "array",
      "items": { "type": "string" },
      "default": [],
      "description": "Allowlist of IPs or CIDR ranges. If non-empty, only these IPs are permitted."
    },
    "deny": {
      "type": "array",
      "items": { "type": "string" },
      "default": [],
      "description": "Denylist of IPs or CIDR ranges (IPv4 and IPv6). These IPs are always blocked (takes precedence over allow)."
    },
    "trusted_proxies": {
      "type": "array",
      "items": { "type": "string" },
      "default": [],
      "description": "Exact IPs of trusted reverse proxies. X-Forwarded-For / X-Real-IP are honored only when the immediate peer is one of these; otherwise forwarded headers are ignored so clients cannot spoof their address."
    },
    "message": {
      "type": "string",
      "default": "Access denied",
      "description": "Custom error message returned to denied requests"
    },
    "status": {
      "type": "integer",
      "minimum": 100,
      "maximum": 599,
      "default": 403,
      "description": "HTTP status code returned to denied requests"
    }
  },
  "additionalProperties": false
}
