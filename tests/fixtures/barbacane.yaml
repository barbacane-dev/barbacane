# Test fixtures manifest
# Declares plugins used by the test spec files

plugins:
  mock:
    path: ../../plugins/mock/mock.wasm
  http-upstream:
    path: ../../plugins/http-upstream/http-upstream.wasm
  jwt-auth:
    path: ../../plugins/jwt-auth/jwt-auth.wasm
  apikey-auth:
    path: ../../plugins/apikey-auth/apikey-auth.wasm
